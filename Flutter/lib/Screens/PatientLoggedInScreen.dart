import 'package:flutter/material.dart';


class PatientLoggedInScreen extends StatelessWidget {
  const PatientLoggedInScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
